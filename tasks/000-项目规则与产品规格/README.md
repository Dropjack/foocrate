# 000-项目规则与产品规格

- 状态：规格核对中
- 对应规格：`specs/000-product-spec.md`、`specs/001-ui-ux-goals.md`
- 前置任务：无
- 最后更新：2026-07-13

## 任务目标

建立一个可以跨很多轮继续、每一步都能由用户检查的项目基础。完成本任务后，项目应具备可靠的中文读取规则、Git 边界、只读参考证据、产品总规格和 UI/UX 目标，但仍不进入 C++ 实现。

## 明确不做

- 本任务不创建 Visual Studio/CMake 工程，不编译组件。
- 本任务不写任何用户功能的 C++ 阶段性代码。
- 本任务不修改、清理或部署到 `D:\Dev\foobar2000`。
- 本任务不替用户批准产品范围，也不把脚本推断当作已经观察确认的行为。

## 可检查步骤

### 步骤 1：长期协作与编码规则

- 输入：用户对长周期、中文教学、逐步检查和完整实现的要求；原有项目规则文件。
- 动作：补充启动协议、UTF-8 无 BOM、中文逻辑、任务状态、完整实现和验收规则；增加自动发现入口和编辑器约束。
- 产物：`AGENTS.md`、`docs/PROJECT_RULES.md`、`.editorconfig`。
- 用户检查方法：阅读规则，确认以后每轮都先读规则、任务索引和当前任务；确认没有 MVP 或占位实现的许可。
- 通过标准：规则准确表达用户意图，文件严格为 UTF-8 无 BOM。
- 状态：已完成，等待用户检查

### 步骤 2：只读参考盘点

- 输入：`D:\Dev\foobar2000` 中的 Foobox 安装、配置和脚本。
- 动作：只读识别版本、核心面板、配置数据和外部组件；区分文件证据与界面观察。
- 产物：[`参考盘点.md`](参考盘点.md)。
- 用户检查方法：核对清单是否覆盖平时使用的主要区域，并指出哪些行为最重要。
- 通过标准：没有修改参考目录；所有未实际观察的结论都明确标为待观察。
- 状态：已完成首轮盘点，等待用户检查

### 步骤 3：Git 基础

- 输入：当前项目目录及已有第三方源码。
- 动作：初始化 `main` 分支；建立本地实例、构建产物和临时文件的忽略规则；约束文本换行。
- 产物：`.git/`、`.gitignore`、`.gitattributes`。
- 用户检查方法：在 Fork 中打开 `D:\Dev\Refrain`，应看到 `main` 分支和待提交文件，不应看到 `.local` 或构建垃圾。
- 通过标准：Git 状态可读，未自动提交，未改动第三方文件。
- 状态：已完成，等待用户检查

### 步骤 4：任务系统

- 输入：用户要求每一步都可检查，项目会跨多轮继续。
- 动作：建立唯一任务入口、状态定义、本轮任务包和可复用模板。
- 产物：`tasks/README.md`、本目录、`tasks/_模板/README.md`。
- 用户检查方法：从 `tasks/README.md` 能在一分钟内知道当前任务、状态、下一步和要检查的文档。
- 通过标准：没有依赖聊天历史才能理解的关键状态。
- 状态：已完成，等待用户检查

### 步骤 5：产品与 UI/UX 规格

- 输入：项目目标、Foobox 首轮文件证据和完整实现原则。
- 动作：用中文写产品边界、模块逻辑、完整可用条件、界面目标、交互状态和待观察问题。
- 产物：`specs/000-product-spec.md`、`specs/001-ui-ux-goals.md`。
- 用户检查方法：按文档内“用户核对清单”逐项提出同意、修改或删除。
- 通过标准：规格没有把待确认内容写成定论；用户理解并批准后才改为“可实现”。
- 状态：已完成草案，等待用户核对

### 步骤 6：本轮完整性核验

- 输入：本轮全部新增和修改文件。
- 动作：验证 UTF-8 无 BOM、Git 状态、参考目录未改动和文档链接。
- 产物：本文件的验证记录。
- 用户检查方法：查看验证记录中的实际命令结论。
- 通过标准：全部项目通过，或明确列出需要修复的项目。
- 状态：已完成，等待用户检查

## 本轮用户检查点

- [ ] 用户认可 `docs/PROJECT_RULES.md` 对长期合作方式的描述。
- [ ] 用户确认参考盘点覆盖了主要日常使用区域，或补充遗漏区域。
- [ ] 用户能在 Fork 中正常看到仓库和 `main` 分支。
- [ ] 用户逐项核对产品规格，指出同意、修改或待观察项。
- [ ] 用户逐项核对 UI/UX 目标，确定默认布局和优先观察行为。

## 验证记录

- 2026-07-13 编码检查：对 `.editorconfig`、`.gitattributes`、`.gitignore`、`AGENTS.md`、`docs/PROJECT_RULES.md`、`tasks/` 和 `specs/` 的全部项目文本读取原始字节，并用启用错误检测的 UTF-8 解码。结果全部有效。
- 2026-07-13 BOM 检查：上述文件均不以 `EF BB BF` 开头，结果全部为 UTF-8 无 BOM。
- 2026-07-13 换行检查：上述文件的 CRLF 计数均为 0，使用 LF 换行。
- 2026-07-13 Git 检查：仓库位于 `D:\Dev\Refrain\.git`，当前分支为 `main`，尚无提交；本轮文件和原有 `third_party/` 均保持为待跟踪状态，留给用户在 Fork 中检查后决定首次提交。
- 2026-07-13 参考目录边界：本轮只执行目录、文本和原始字节读取，没有对 `D:\Dev\foobar2000` 执行写入、部署、清理或启动后保存配置。

## 决策与未决问题

- 已决定：项目不采用 MVP 或不可用的阶段性功能作为交付方式。
- 已决定：面向用户的逻辑解释全部使用中文，C++ 实现必须有对应中文逻辑。
- 已决定：参考目录严格只读，日常 C 盘安装严格不接触。
- 待用户决定：第一套默认布局在 Foobox 的 `basic`、`+ biography`、`+ visualization` 或组合布局中以哪一个为精确基准。
- 待用户决定：歌词、Biography、频谱等依赖外部组件的区域，是由 Refrain 原生实现，还是作为经批准的可选嵌入区域。
