# Foobox 参考证据

- 参考目录：`D:\Dev\foobar2000`
- 参考版本：foobar2000 2.25.10
- 初次盘点：2026-07-13
- 边界：只读检查；文件证据不等同于已经实际操作确认

## 证据等级

- **文件事实**：可由版本、路径、配置值或脚本直接证明。
- **文件推断**：脚本显示存在该行为，但仍需在界面中实际验证。
- **产品决定**：用户已经决定 Refrain 怎么做，不表示 Foobox 行为已经观察完成。

## 已确认文件事实

- `profile/version.txt` 记录 foobar2000 2.25.10。
- `themes/` 包含 `foobox8 basic.fth`、Biography/visualization 组合及深浅变体。
- 用户选择 `foobox8 basic.fth` 和已提供截图作为 Refrain 第一套结构基准。
- `profile/foobox/config/columns` 包含封面、状态、序号、音轨号、标题、日期、艺术家、专辑艺术家、专辑、流派、Mood、Rating、播放次数、码率、编码、采样率和时长等参考列。
- 分组配置包含简易专辑、专辑艺术家/专辑/碟号、专辑艺术家、艺术家、流派和目录。
- 当前 `miscconf` 的媒体库按钮值为 `1`；`base.js` 对应执行 `Library/Album List`，值为 `0` 才执行 ReFacets。
- `infoArt.js` 显示标题，并在“歌手 | 专辑”和“编码 | 编码配置 | 比特率 | 采样率”之间按默认 12 秒轮换；Refrain 已决定改为两条同时显示。
- `infoArt.js` 的中键处理在 ESLyric 与属性列表间切换；播放开始/停止还存在自动切换线索。
- 五星操作调用 `Playback Statistics/Rating/N`；参考配置的 `rating2tag=0`，即默认不同时写文件 `RATING` 标签。
- 参考环境存在 ESLyric 1.0.6.7、Playback Statistics 3.1.10、JSplitter、Enhanced Spectrum Analyzer、OpenHacks 等组件。
- 参考目录存在 Biography 缓存和可视化布局，但它们只证明参考环境安装过，不代表 Refrain 要实现。

## 已由用户解决的产品选择

- 保留 Windows 原生标题栏，不复刻隐藏系统标题栏。
- 媒体库入口使用 Album List，不使用 ReFacets 或 Refrain 自建媒体库。
- 右上固定显示标题和两条次要信息，不轮换。
- 右下默认 ESLyric，中键切换只读歌曲信息；歌词双击交给 ESLyric 全屏。
- 评分继续使用 Playback Statistics `%rating%`，默认不写文件标签。
- Biography、内嵌频谱和收音机排除；频谱若由用户另装，只从 foobar2000 菜单访问。
- Refrain 不依赖任何 JavaScript UI 宿主；ESLyric 内部 QuickJS 只批准用于歌词源和解析。

## 步骤 01 必须实际观察

1. 底部左右每个图标的命令、提示、禁用条件和右键行为。
2. 左栏搜索范围、筛选入口、新建/重命名/删除/排序和自动列表保护。
3. 播放、暂停、停止、网络流和无焦点时各区域显示。
4. 右上封面/信息跟随正在播放项或列表焦点的精确规则。
5. 多封面循环、双击、右键、刷新、缓存和缺图行为。
6. ESLyric 双击全屏的设置前提与退出方式；中键/自动切换的实际状态规则。
7. 普通、最窄、最大化和不同 DPI 下的三栏比例与收缩方式。
8. 播放列表分组、列、选择、队列、拖放和右键的精确行为。
9. 默认配色、字体、图标、强调色传播和所有交互状态。

## 观察方法

- 每项记录参考版本、入口、初始状态、操作步骤、实际结果和必要截图。
- 将“实际观察”“文件推断”“Refrain 目标”分栏记录，不能互相替代。
- 只有能保证不写回参考目录时才启动参考；否则由用户观察并反馈，或先提出安全隔离方案。
- 原始截图若需跨会话逐像素使用，应由用户放入仓库约定的参考资料目录；聊天附件本身不是仓库证据。
