# Foobox 参考项目首轮盘点

- 参考根目录：`D:\Dev\foobar2000`
- 参考应用版本：foobar2000 v2.25.10
- 盘点日期：2026-07-13
- 盘点方式：仅只读检查目录、配置和脚本；本轮未把界面实际操作结果写成事实

## 证据等级

- 已确认文件事实：可以直接由版本文件、目录、配置或脚本名称证明。
- 由文件推断，待观察：脚本和布局文件强烈表明存在，但仍需启动参考界面操作确认外观、位置和细节。
- 尚未确认：需要用户说明或后续专门观察。

## 已确认文件事实

- `profile/version.txt` 记录版本为 foobar2000 v2.25.10。
- `themes/` 中存在 `foobox8 basic.fth`、`foobox8 + biography.fth`、`foobox8 + visualization.fth`、`foobox8 + biography + visualization.fth` 以及深浅色变体。
- `profile/foobox/config/` 保存播放列表列、分组、布局和通用设置；可见列包含封面、状态、序号、音轨号、标题、日期、艺术家、专辑艺术家、专辑、流派、心情、评分、播放次数、码率、编码、采样率和时长。
- 分组配置包含简易专辑、专辑艺术家/专辑/碟号、专辑艺术家、艺术家、流派和目录等方式。
- Foobox 面板脚本包括基础控制区、封面与信息、播放列表、封面浏览、播放列表管理、多播放列表查看、单播放列表查看、属性和搜索。
- 基础控制脚本包含文件/编辑/视图/播放/媒体库/帮助菜单、打开/上一首/播放暂停/下一首/停止、播放进度、音量、静音、播放顺序、输出设备、媒体库入口和窗口最小化/最大化/关闭的行为线索。
- 参考环境安装了 `foo_uie_jsplitter`、`foo_uie_eslyric`、`foo_enhanced_spectrum_analyzer`、`foo_openhacks_mod` 和 `foo_playcount`。
- 参考环境含专辑封面默认图、流派图片、Foobox 配置以及 Biography 缓存数据。

## 由文件推断，必须实际观察后确认

- 顶部区域可能同时承担传统菜单、播放曲目信息、窗口拖动区和自绘窗口按钮。
- 播放控制、时间、进度、媒体库/播放顺序/输出/静音、音量和面板切换可能处于同一条主控制带。
- 主内容区域可能在媒体库封面浏览、播放列表管理、播放列表、封面信息、歌词、Biography 和频谱面板之间切换或分割显示。
- 横向和纵向分隔条支持拖动，位置可能被保存并在下次启动恢复。
- 播放列表支持分组、封面、列宽调整、排序、选择、拖放、右键菜单、键盘操作、评分/心情和播放状态显示。
- 封面浏览支持筛选、异步封面加载、缓存、缺图回退和双击/拖放到播放列表。
- 封面信息区可能跟随正在播放项或当前焦点项，并可在封面、信息、歌词等内容间自动或手动切换。

## 尚未确认且需要优先观察

1. 用户平时使用的是哪一个 `.fth` 布局，以及启动后的默认窗口尺寸和各区域比例。
2. 顶部控制带与主内容区的精确上下顺序、尺寸、间距和响应式变化。
3. 媒体库按钮打开 Album List 还是 ReFacets，以及用户实际使用哪一种。
4. 播放列表管理器、封面浏览、播放列表之间的导航方式和默认选中项。
5. 窄窗口、最小窗口、最大化、高 DPI 和多显示器切换时的布局行为。
6. 无音乐、无封面、无媒体库、网络流、播放停止和文件失效时显示什么。
7. 深色/浅色主题的来源、强调色传播方式和用户是否经常切换。
8. 歌词、Biography、频谱哪些是目标核心，哪些只是当前参考安装中的附加面板。
9. 所有鼠标、键盘、拖放、右键菜单和提示文本的精确行为。

## 后续观察方法

后续每个 UI 模块建立独立观察任务：先记录参考主题和初始状态，再逐项操作并截图，最后把“观察结果—目标行为—验收步骤”三者对应起来。观察只能在参考实例中进行，任何配置变化都不得保存回参考目录；如某项观察必然会写配置，应先复制到允许的本地测试实例再操作。
